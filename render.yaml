services:
  - type: web
    name: kucoin-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      TELEGRAM_TOKEN: 
        generateValue: true
