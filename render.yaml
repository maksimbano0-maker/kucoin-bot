services:
  - type: web
    name: kucoin-bot
    env: python
    pythonVersion: "3.11"  # ← КРИТИЧНО!
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
    envVars:
      TELEGRAM_TOKEN:
        sync: false
